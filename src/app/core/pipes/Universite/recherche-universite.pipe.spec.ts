import { RechercheUniversitePipe } from './recherche-universite.pipe';

describe('RechercheUniversitePipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheUniversitePipe();
    expect(pipe).toBeTruthy();
  });
});
